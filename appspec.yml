version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "course-service-task:1"
        LoadBalancerInfo:
          ContainerName: "course-service"
          ContainerPort: 80
